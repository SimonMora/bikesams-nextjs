import { JoinLayout } from "@/layouts";
import { ConfirmForm } from '@/components/Auth';

export default function Register() {
  return (
    <JoinLayout>
        <ConfirmForm />
    </JoinLayout>
  )
}